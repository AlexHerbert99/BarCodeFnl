@model IEnumerable<barCode.Models.Categoria>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:white">Categorias</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover" style="background-color:wheat">
    <tr>
        <th>idCategoria</th>
        <th>
            Categoria
        </th>
        <th>Opciones</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdCategoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdCategoria }) |            
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdCategoria })
        </td>
    </tr>
}

</table>
